let bindInfo = {}
if(bindInfo.active === false){
    // 进入绑定
}else{
    if(bindInfo.binderId && bindInfo.isBindDeleted === false){
        // 进入注册
    }else{
        if(bindInfo.openId !== fromID){
            // 进入关注
        }else{
            // 进入已关注
        }
    }
}